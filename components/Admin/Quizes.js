import { useContext } from "react";
import styled from "styled-components";

import AppContext from "../../context/AppContext";

export default function Quizes() {
  return <Container>Quizes</Container>;
}

const Container = styled.div``;
